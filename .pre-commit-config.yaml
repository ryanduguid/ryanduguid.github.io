# Local git hooks. Install once with `pre-commit install`. Ruff matches CI.
# This hook runs lint only; CI also checks formatting.
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.8
    hooks:
      - id: ruff

