{
  "name": "duguid-site-quality",
  "private": true,
  "type": "module",
  "scripts": {
    "test:browser": "playwright test",
    "test:browser:update": "playwright test --update-snapshots",
    "test:capture": "playwright test --config=playwright.capture.config.mjs",
    "capture:coal-lsl-proof": "playwright test --config=playwright.capture.config.mjs --update-snapshots=all",
    "pretest:lighthouse": "node --test scripts/lighthouse-config.test.cjs && node scripts/clean-lighthouse-output.mjs",
    "test:lighthouse": "lhci autorun"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@lhci/cli": "0.15.1",
    "@playwright/test": "1.62.1"
  },
  "overrides": {
    "tmp": "0.2.7",
    "uuid": "11.1.1"
  }
}
